# LP build artifacts & local env
dist/
node_modules/
.env
.env.*
!.env.example
!.env.dev.sample
!.env.prod.sample

# CFN deploy artifact — regenerated fresh by infra/deploy.sh on every run,
# never hand-edited. See reality-flow-authentication/.gitignore, server/.gitignore.
infra/cfn-params.json
