# Missing API Gateway Routes - Add these to apigw-explicit-routes.yaml
# This file documents the 24 missing routes that need to be added to the CFN template
# Run: cat missing-routes-cfn.yaml >> apigw-explicit-routes.yaml (after removing the comment lines)

# Missing routes found by find-missing-apigw-routes.js:
# 1. POST /api/crm/buyers/:id/list-property
# 2. POST /api/crm/contacts/migrate/all
# 3. POST /api/crm/contacts/migrate/customer/:customerId
# 4. POST /api/crm/contacts/migrate/owner/:ownerId
# 5. POST /api/crm/customers/:id/archive-rental
# 6. PUT /api/crm/customers/:id/current-rental
# 7. GET /api/crm/customers/:id/rental-history
# 8. GET /api/crm/meetings/upcoming
# 9. GET /api/crm/properties/:id/rental-history
# 10. GET /api/crm/properties/list/detailed
# 11. PUT /api/enquiries/:id/close
# 12. PUT /api/enquiries/:id/reopen
# 13. POST /api/enquiries/consultation
# 14. POST /api/enquiries/contact
# 15. POST /api/notifications/:notificationId/read
# 16. DELETE /api/notifications/cleanup
# 17. GET /api/notifications/counts
# 18. POST /api/notifications/generate-rent-expiry
# 19. POST /api/notifications/mark-all-read
# 20. POST /api/notifications/process-all
# 21. POST /api/notifications/process-scheduled
# 22. GET /api/notifications/settings
# 23. PUT /api/notifications/settings
# 24. POST /api/notifications/test

# RECOMMENDATION: Use a script to generate these resources programmatically
# instead of manually adding 500+ lines of YAML boilerplate.
# Each route needs: Resource + GET/POST/PUT/DELETE Method + OPTIONS Method
# That's 3 resources per route minimum, with full MethodResponses for each.

# For now, the most critical routes are:
# - Contact activity (DONE)
# - Contact role filters: owners, sellers, buyers, tenants (DONE)
# - Buyer list-property
# - Contact migrate operations
# - Customer rental history & archive
# - Property rental history
# - Meetings upcoming
# - Enquiry close/reopen
# - Notification actions (counts, settings, read, cleanup, etc.)
