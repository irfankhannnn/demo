{
  "name": "realestateflow-lp-build",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Vite + Tailwind build pipeline for RealEstateFlow landing pages. Compiles Tailwind CSS, expands {{> partial}} includes, and injects build-time env vars into dist/.",
  "scripts": {
    "build:lps": "vite build && node scripts/process-partials.js",
    "dev:lps": "vite preview --port 5000",
    "clean": "rimraf ../dist"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "dotenv": "^16.4.5",
    "postcss": "^8.4.38",
    "rimraf": "^5.0.7",
    "tailwindcss": "^3.4.4",
    "vite": "^5.3.1"
  }
}
