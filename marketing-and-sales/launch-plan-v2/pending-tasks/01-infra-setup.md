# 01 — Infrastructure Setup

---

## INFRA-01: Create 7 New DynamoDB Tables
**Priority:** Critical — Blocks all coding PRs from deploying
**Status:** [x] AI-complete (CloudFormation) — 2026-06-11 · Human deploy pending

CloudFormation template created at `server/infra/launch-tables-cfn.yaml` with all 7 tables, PITR, GSIs, and WebhookLog TTL.

**Founder deploy command:**
```bash
aws cloudformation deploy \
  --template-file server/infra/launch-tables-cfn.yaml \
  --stack-name realestateflow-launch-tables \
  --region ap-south-1
```

**Files Changed:** `server/infra/launch-tables-cfn.yaml`

**PR Reference:** `cursor/pending-tasks-consolidation-492f`

Tables required (not yet in existing CFN):
`Grievances`, `AIEmployeeProvisioning`, `WebhookLog`, `TenantApiKeys`, `Subscriptions`, `NPSResponses`, `BetaInvites`

- Log into AWS Console → DynamoDB → `ap-south-1`
- Create each table with schemas from `coding-agent-brief/01-SHARED-CONTRACTS.md §1`
- Enable PITR on all 7 tables
- Set BillingMode = PAY_PER_REQUEST

**References:**
- Schema specs: `coding-agent-brief/01-SHARED-CONTRACTS.md §1.1–1.7`
- Source task: `pre-launch-prep/P18-cloud-infra-checklist.md` Track A §3
- Coding PRs that need these live: PR-B (Grievances), PR-F (AIEmployeeProvisioning, WebhookLog, TenantApiKeys), PR-H (Subscriptions), PR-K (NPSResponses)

---

## INFRA-02: Create Demo Cognito User Pool
**Priority:** High — Blocks demo env deployment (PR-A YAML)
**Status:** [ ] Human-dependent — Updated 2026-06-11

### Current Status
Awaiting Founder AWS Console action. PR-A cron YAML and seed scripts are merged.

### Updated Implementation Guidance (2026)
- [AWS Cognito User Pools](https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-pools.html): create pool in `ap-south-1`; use `ALLOW_USER_SRP_AUTH`; optional MFA
- Password policy: minimum 10 characters per P5 spec
- After creation: add `DEMO_USER_POOL_ID`, `DEMO_CLIENT_ID`, `DEMO_TENANT_ID` to Lambda env (never commit)

### Notes For Human Owner
Demo SPA build uses `VITE_IS_DEMO=true` + demo pool IDs. Seed once via `node server/scripts/seed-demo-tenant.js --reset`.

- AWS Console → Cognito → Create user pool `realestateflow-demo`
- Config: MFA optional, SRP_AUTH, password ≥10 chars
- Create user `demo@realestateflow.in` with permanent password
- Capture `DEMO_USER_POOL_ID`, `DEMO_CLIENT_ID` → add to Lambda env vars
- Set `DEMO_TENANT_ID` env var (any fixed string, e.g. `DEMO_REALESTATEFLOW`)

**References:**
- `team-work/FOUNDER-tasks.md` FND-007-T1/T2
- `pre-launch-prep/P5-demo-environment.md` Manual Steps §1-2
- Coding PR that needs these: PR-A (`cron/reset-demo.yaml` EventBridge deployment)

---

## INFRA-03: Deploy Lambda Cron Functions
**Priority:** High — Blocked on PR-A, PR-F, PR-J being merged first

After PRs are merged, deploy 3 Lambda cron functions using the YAML files created by agents:
- `cron/reset-demo.yaml` → `cron(30 21 * * ? *)` (2:00 AM IST daily)
- `cron/escalate-openclaw.yaml` → `rate(6 hours)`
- `cron/trial-reminder.yaml` → `cron(30 3 * * ? *)` (9:00 AM IST daily)

- AWS Console → Lambda → Create function for each
- Set EventBridge schedule from each YAML file
- Verify by manually invoking each Lambda once; confirm no errors in CloudWatch

**References:**
- `team-work/FOUNDER-tasks.md` FND-002-T6/T7
- `pre-launch-prep/P18-cloud-infra-checklist.md` Track A §6-7
- Depends on: PR-A, PR-F, PR-J merged

---

## INFRA-04: API Gateway Rate Limiting + WAF Rules
**Priority:** High — Security requirement before public launch

- API Gateway: add per-route throttling (100 req/5min) on `/api/grievance`, `/api/auth/phone-login`, `/api/billing/webhook`
- WAF: attach to API Gateway; enable CommonRuleSet + KnownBadInputsRuleSet + AmazonIpReputationList
- Add custom rate-limit rule: >100 req/5min per IP on grievance + phone-login → BLOCK

**References:**
- `team-work/FOUNDER-tasks.md` FND-002-T8/T9
- `coding-agent-brief/03-ANTI-CONFLICT-RULES.md` (alarm specs)
- Alarm spec: `pre-launch-prep/P13-multitenancy-security-audit.md`

---

## INFRA-05: CloudWatch Alarms
**Priority:** High — Required before Day 1

Create 5 alarms per the spec in `launch-implement/pre-launch/13-security/sentry-cloudwatch-alarms.md` (generated by PR-G):
- Lambda 5xx rate >1%/min → SNS email
- Lambda error count >5/5min → SNS
- DDB ThrottledRequests > 0 for 3 periods → SNS
- Cognito sign-in failures >10/5min same IP → SNS
- API Gateway 4xx rate >5%/min → SNS

**References:**
- `team-work/FOUNDER-tasks.md` FND-002-T10
- Alarm spec file: `marketing-and-sales/launch-implement/pre-launch/13-security/sentry-cloudwatch-alarms.md` (created by PR-G)

---

## INFRA-06: Cloudflare DNS Setup
**Priority:** Critical — Blocks email deliverability and all domain resolution

- Add `realestateflow.in` zone; update registrar nameservers
- Add records from `pre-launch/03-deliverability/dns-records.md` (generated by MAD-003 AI prompt):
  - SPF, DKIM (Google Workspace + Brevo + Instantly), DMARC
  - MX records (Google Workspace)
  - A/CNAME: `realestateflow.in` → Netlify LP, `app` → Netlify CRM, `api` → API Gateway, `demo` → Netlify demo, `status` → BetterStack
- Zone settings: SSL/TLS Full (strict), Always HTTPS, Bot Fight Mode ON
- Email DNS records must use DNS-only (orange cloud OFF)

**References:**
- `team-work/FOUNDER-tasks.md` FND-003
- `pre-launch-prep/P3-email-deliverability.md`
- `pre-launch-prep/P18-cloud-infra-checklist.md` Track B

---

## INFRA-07: Populate All Env Vars
**Priority:** Critical — Required before any deployment goes live

After all vendor accounts are created (see `02-external-accounts.md`), populate:
- `creative/landing-pages/.env` → GA4_ID, META_PIXEL_ID, LINKEDIN_PARTNER_ID, HOTJAR_ID, POSTHOG_KEY
- `real-estate-crm-app/.env` → VITE_POSTHOG_KEY, VITE_SENTRY_DSN, VITE_RAZORPAY_KEY_ID, VITE_HCAPTCHA_SITE_KEY, VITE_IS_DEMO=false
- Lambda env vars → POSTHOG_KEY_SERVER, SENTRY_DSN_SERVER, RAZORPAY_WEBHOOK_SECRET, BREVO_API_KEY + all template IDs, HCAPTCHA_SECRET_KEY, AISENSY_API_KEY, AISENSY_BROADCAST_LIST_ID

Store raw values in 1Password/Bitwarden — never commit to git.

**References:**
- `coding-agent-brief/00-MASTER-BRIEF.md §9`
- `team-work/FOUNDER-tasks.md` FND-005-T7, FND-006-T6
- `creative/landing-pages/.env.example` (created by PR-D)
