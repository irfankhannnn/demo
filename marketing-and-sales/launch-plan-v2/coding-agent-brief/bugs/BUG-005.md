# Seven Launch DynamoDB Tables Not Defined in CloudFormation

> **AUDIT 2026-06-13 — RESOLVED on `auth_rbac_feature` (code/IaC).** `apps/crm/server/infra/launch-tables-cfn.yaml` defines all 7 tables (`Grievances`, `AIEmployeeProvisioning`, `WebhookLog`, `TenantApiKeys`, `Subscriptions`, `NPSResponses`, `BetaInvites`) with PITR, GSIs, and WebhookLog TTL. Remaining step is the human `aws cloudformation deploy` (INFRA-01) — tracked in Phase 3/5, not a code action.

## Merge Status (auth_rbac_feature)
| Field | Value |
|---|---|
| Source | Manual infra (`pending-tasks/01-infra-setup.md`) — **not a PR deliverable** |
| PR code on auth_rbac | ✅ Services reference tables via env vars |
| CFN on auth_rbac | ❌ Tables not in `apps/crm/server/infra/` |
| Merge blocker | **No** — founder/manual task before API integration tests |
| Re-merge needed | **No** |

## Epic
FND-002 / INFRA-01 — AWS Infrastructure

## Story
`01-SHARED-CONTRACTS.md §1` — 7 new tables required before coding PRs deploy

## Requirement
Tables: `Grievances`, `AIEmployeeProvisioning`, `WebhookLog`, `TenantApiKeys`, `Subscriptions`, `NPSResponses`, `BetaInvites` with schemas, GSIs, PITR per shared contracts.

## Current State
- Grep of `apps/crm/server/infra/` and repo `*.tf` files: **no references** to these table names.
- Services reference tables via env vars (e.g. `SUBSCRIPTIONS_TABLE`, `GRIEVANCES_TABLE`) with string defaults.
- `pending-tasks/01-infra-setup.md` INFRA-01 lists manual console creation — not done in code.

Evidence: `apps/crm/server/subscriptionService.js:11` → `TABLE_NAME = process.env.SUBSCRIPTIONS_TABLE || 'Subscriptions'`

## Expected State
Either CFN resources in `apps/crm/server/infra/cfn-backend.yaml` (or separate stack) **or** confirmed deployed tables in AWS matching shared-contract schemas.

## Gap Analysis
Code assumes tables exist; infrastructure-as-code does not provision them. Runtime will throw `ResourceNotFoundException` on first API call in any fresh environment.

## Impact
- **User:** Grievance form, billing, NPS, seat checks all fail with 500 errors.
- **Business:** Launch blocked until manual DDB creation.
- **Technical:** No reproducible environment; staging/prod drift.

## Required Changes
1. Add 7 DynamoDB table resources to CFN with GSIs per `01-SHARED-CONTRACTS.md`.
2. Wire table names to Lambda env vars in deployment params.
3. Document in `pending-tasks/01-infra-setup.md` if manual path chosen — mark INFRA-01 status.

## Acceptance Criteria
- `aws dynamodb describe-table` succeeds for all 7 tables in target account.
- Smoke test: POST `/api/grievance` creates row; GET `/api/subscriptions/current` returns data.

## Files Likely Impacted
- `apps/crm/server/infra/cfn-backend.yaml`
- `apps/crm/server/infra/cfn-params.json`
- `pending-tasks/01-infra-setup.md`

## Priority
P0 (infra — required before live API tests; not PR merge)

## Risk
High
