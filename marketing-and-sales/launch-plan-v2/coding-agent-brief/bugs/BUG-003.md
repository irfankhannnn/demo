# `tests/analytics.spec.ts` Missing — ZEE-003-T7 and ZEE-012 Falsely Marked Complete

## Epic
ZEE-003 + ZEE-012 / PR-E + PR-M

## Story
ZEE-003-T7 — Write `tests/analytics.spec.ts`
ZEE-012-T4 — Run analytics spec 100% pass
ZEE-012-T5 — Add analytics spec to CI as required check

## Requirement
`PR-E-analytics-layer.md` and `PR-M` require `tests/analytics.spec.ts` covering:
- LP: Accept cookies → PostHog + GA4 + Pixel + LinkedIn + Hotjar load
- LP: Reject → marketing trackers blocked
- CRM: signup → PostHog identify; zero GA4/Pixel in CRM
- PII safety assertions

## Current State
- File `tests/analytics.spec.ts` **does not exist** (confirmed via glob and `npx playwright test --list` — only 6 spec files, 35 tests).
- `team-work/ZEESHAN-tasks.md` marks ZEE-003-T7 as `[ ]` incomplete but ZEE-012-T4/T5 marked `[x]` complete.
- `output/epics/EPIC-13-COMPLETION.md` admits: "Analytics spec from PR-E is referenced but doesn't exist on this branch yet."
- `.github/workflows/playwright.yml` runs `npx playwright test` on all `tests/*.spec.ts` — analytics spec is silently absent.

## Expected State
`tests/analytics.spec.ts` exists with ≥8 test cases per PR-E spec; CI fails if analytics tests fail.

## Gap Analysis
PR-M merged documentation and CSV audit without delivering the actual test file. Tracker checkboxes do not match repository state.

## Impact
- **User:** Analytics regressions undetected (wrong trackers in CRM, consent bypass).
- **Business:** Funnel data unreliable at launch.
- **Technical:** No automated gate for architecture rule "PostHog only in CRM."

## Required Changes
1. Create `tests/analytics.spec.ts` per `PR-E-analytics-layer.md`.
2. Update CI to explicitly require analytics spec (or fail if missing).
3. Correct ZEE-012 tracker checkboxes to reflect actual state until fixed.

## Acceptance Criteria
- `npx playwright test tests/analytics.spec.ts` — 100% pass with API + preview server running.
- CRM network intercept confirms zero `googletagmanager.com` / `facebook.net` requests.
- LP tests confirm consent gating works.

## Files Likely Impacted
- `tests/analytics.spec.ts` (new)
- `.github/workflows/playwright.yml`
- `marketing-and-sales/launch-plan-v2/team-work/ZEESHAN-tasks.md`

## Priority
P0

## Risk
High
