# `billing.js` Uses Stub `incrementSeatsPaid` — Seat Webhook Never Updates DDB

> **AUDIT 2026-06-13 — RESOLVED on `auth_rbac_feature`.** `billing.js:6` imports the real `incrementSeatsPaid` from `../subscriptionService.js` and calls it in the `subscription.updated` handler (`:258`). Local stub removed. No action required.

## Merge Status (auth_rbac_feature)
| Field | Value |
|---|---|
| Source PRs | PR-F (billing.js stub) + PR-H (subscriptionService — never wired into billing.js) |
| Merged into auth_rbac | ✅ Both merged (`1a21fd5`, `385af00`) |
| PR-F tip has same stub | ✅ Identical stub on PR-F and auth_rbac — not a merge regression |
| PR-H tip has billing.js | ❌ PR-H branch never contained `billing.js` — cross-PR wiring gap |
| Merge blocker | **No** |
| Re-merge needed | **No** — fix import on auth_rbac_feature |

## Epic
ZEE-004 + ZEE-005 / PR-F + PR-H

## Story
ZEE-004-T1 (subscription.updated branch) + ZEE-005-T1 (`subscriptionService.incrementSeatsPaid`)

## Requirement
On `subscription.updated` Razorpay webhook, call `incrementSeatsPaid(tenantId)` from `agency-app/api/subscriptionService.js` to update the `Subscriptions` table.

## Current State
`agency-app/api/routes/billing.js:10-13` defines a local stub:

```10:13:agency-app/api/routes/billing.js
async function incrementSeatsPaid(tenantId, by) {
  logger.info('[subscriptionService stub] incrementSeatsPaid', { tenantId, by });
}
```

`agency-app/api/subscriptionService.js:28-40` has the real implementation but is **never imported** by `billing.js`. Line 245 calls the local stub.

## Expected State
`billing.js` imports `{ incrementSeatsPaid } from '../subscriptionService.js'` and calls it on seat-add webhook events.

## Gap Analysis
PR-F intentionally left a stub ("PR-H creates the real subscriptionService"). PR-H delivered `subscriptionService.js` but **did not update `billing.js`** per `PR-H-seat-cap.md` spec. Merge into `auth_rbac_feature` correctly included both files but the cross-PR wiring step was never implemented in either PR.

## Impact
- **User:** Pays for extra seat; invite still blocked at old cap.
- **Business:** Revenue collected without service delivery — support burden + refund risk.
- **Technical:** `Subscriptions.seatsPaid` drifts from Razorpay reality.

## Required Changes
1. Remove stub in `billing.js`; import real `incrementSeatsPaid`.
2. Add unit/integration test: mock webhook `subscription.updated` → assert DDB `seatsPaid` incremented.

## Acceptance Criteria
- `subscription.updated` with seat add payload increments `Subscriptions.seatsPaid` by 1.
- `POST /api/subscriptions/check-seat` reflects new cap after webhook.

## Files Likely Impacted
- `agency-app/api/routes/billing.js`
- `tests/seat-cap.spec.ts`

## Priority
P0 (testing phase — not a merge blocker)

## Risk
High
