# Server Lambda Missing Sentry Initialization

## Merge Status (auth_rbac_feature)
| Field | Value |
|---|---|
| Source PR | PR-E |
| Merged into auth_rbac | ✅ CRM Sentry in `main.tsx`; server Sentry not in PR-E deliverable |
| PR-E tip has lambda Sentry | ❌ Not on PR-E branch |
| Merge blocker | **No** |
| Re-merge needed | **No** |

## Epic
ZEE-003 / PR-E

## Story
ZEE-003-T6 — Wire Sentry into CRM + Server (`lambda-handler.js`)

## Requirement
`00-MASTER-BRIEF.md §5`: Sentry in `src/main.tsx` + `server/lambda-handler.js`.

## Current State
- `real-estate-crm-app/src/main.tsx:9-15` — Sentry initialized ✅
- `server/lambda-handler.js` — **no Sentry import or init** (grep `Sentry|posthog` → no matches)

`ZEESHAN-tasks.md` marks T6 complete with note "server Lambda Sentry is out of scope per PR-E spec" — contradicts master brief.

## Expected State
`lambda-handler.js` initializes `@sentry/node` with `SENTRY_DSN_SERVER` before wrapping Express.

## Gap Analysis
CRM errors tracked; server/Lambda errors only in CloudWatch logs. Production exceptions in billing webhook, grievance, cron handlers invisible in Sentry.

## Impact
- **User:** Slower incident response on payment failures.
- **Business:** Billing bugs may go unnoticed until customer complaints.
- **Technical:** Split observability — incomplete error tracking.

## Required Changes
1. Add `@sentry/node` to `server/package.json` if missing.
2. Init Sentry at top of `lambda-handler.js`.
3. Verify error capture on thrown exception in webhook handler.

## Acceptance Criteria
- Test exception in Lambda appears in Sentry project within 60s.
- No Sentry init in LP HTML files.

## Files Likely Impacted
- `server/lambda-handler.js`
- `server/package.json`

## Priority
P2 (testing phase)

## Risk
Medium
