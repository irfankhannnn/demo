# Local build/release history — see README.md. Durable source of truth for
# the actual artifacts is S3 (versioned); this folder is a local index plus
# instant-access template/param snapshots for rollback. Never commit it —
# it's per-machine deploy history, not project source.
deploy-versions/
