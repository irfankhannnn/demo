{
  "_comment": [
    "ALLOWLIST, not a denylist — fail-closed by design (see",
    "docs/proposals/config-only-deploy/context.md and",
    "platform/auth/infra/config-only-allowed-params.json for the",
    "full rationale). A parameter not named here is unsafe-by-default.",
    "",
    "Deliberately NOT listed, and why:",
    "  EnvironmentName            — identity of the stack itself.",
    "  LambdaRuntime              — changing the Node runtime without a real",
    "                                rebuild against it is not a shortcut to allow.",
    "  LambdaCodeS3Bucket/Key     — LambdaCodeS3Key is a freshly timestamped",
    "                                key only a real deploy's upload produces;",
    "                                config-only mode never uploads anything,",
    "                                so this must always carry forward the",
    "                                live stack's current value.",
    "  AICallingTableName         — these are the PHYSICAL resource names of",
    "  AICallingKnowledgeBucket     the DynamoDB table and S3 buckets this",
    "  AICallingRecordingsBucket    template itself creates (cfn-ai-calling.yaml",
    "                                TableName:/BucketName: !Ref ...) — changing",
    "                                any of them makes CloudFormation try to",
    "                                REPLACE that resource (delete + recreate,",
    "                                real data loss), not a config tweak."
  ],
  "allowedParams": [
    "LambdaMemorySize",
    "LambdaTimeout",
    "CrmInternalApiDomainName",
    "CrmInternalApiBasePath",
    "CrmInternalApiKey",
    "ExotelSubdomain",
    "ExotelApiKey",
    "ExotelApiToken",
    "ExotelSid",
    "ExotelCallerId",
    "ExotelWebhookIps",
    "ElevenLabsApiKey",
    "ElevenLabsAgentId",
    "ElevenLabsAgentPhoneNumberId",
    "ElevenLabsWebhookSecret",
    "ServerToolApiKey",
    "CrmCallerApiKey",
    "ApiStageName",
    "AllowedOrigins",
    "AiCallingApiDomainName",
    "AiCallingApiBasePath",
    "EnableCustomDomainMapping",
    "EnableBasePathStrip",
    "LogLevel",
    "LogRetentionDays",
    "RecordingRetentionDays"
  ]
}
