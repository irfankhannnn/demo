{
  "_comment": [
    "ALLOWLIST, not a denylist — fail-closed by design (see",
    "docs/proposals/config-only-deploy/context.md and",
    "services/reality-flow-authentication/infra/config-only-allowed-params.json for the",
    "full rationale).",
    "",
    "This is a static site (S3 + CloudFront), same shape as",
    "real-estate-crm-app — every CFN parameter is pure infrastructure",
    "topology, nothing app-runtime. 'config-only' (infra/config-deploy.sh,",
    "--skip-build) and 'content-only' (infra/content-deploy.sh, --skip-cfn)",
    "are two INDEPENDENT axes, not one — see",
    "docs/proposals/config-only-deploy/context.md's category 1 vs. category 3.",
    "",
    "Deliberately NOT listed, and why:",
    "  EnvironmentName  — identity of the stack itself.",
    "  BucketName       — the PHYSICAL resource name of the S3 bucket this",
    "                      template creates — changing it makes CloudFormation",
    "                      try to REPLACE the bucket, not a config tweak."
  ],
  "allowedParams": [
    "PriceClass",
    "CustomDomainName",
    "IncludeWwwAlias",
    "WwwIsCanonical",
    "AcmCertificateArn",
    "HostedZoneId",
    "WafWebAclArn"
  ]
}
