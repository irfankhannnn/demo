{
  "_comment": [
    "ALLOWLIST, not a denylist — fail-closed by design (see",
    "docs/proposals/config-only-deploy/context.md and",
    "services/reality-flow-authentication/infra/config-only-allowed-params.json for the",
    "full rationale). A parameter not named here is unsafe-by-default.",
    "",
    "Deliberately NOT listed, and why:",
    "  EnvironmentName            — identity of the stack itself.",
    "  LambdaRuntime              — changing the Node runtime without a real",
    "                                rebuild against it is not a shortcut to allow.",
    "  LambdaCodeS3Bucket/Key     — LambdaCodeS3Key is a freshly timestamped",
    "                                key only a real deploy's upload produces;",
    "                                config-only mode never uploads anything,",
    "                                so this must always carry forward the",
    "                                live stack's current value.",
    "  EnableCloudFront           — flips whether an AWS::CloudFront::",
    "                                Distribution resource exists at all; a",
    "                                real infrastructure topology change, not",
    "                                a tunable, even though no code changes.",
    "  EnableCustomDomainMapping  — creates/removes the BasePathMapping resource;",
    "                                a topology change.",
    "  PagesApiDomainName         — moves the mapping AND the CloudFront origin to",
    "                                another domain; use a full deploy."
  ],
  "allowedParams": [
    "LambdaMemorySize",
    "LambdaTimeout",
    "CrmInternalApiDomainName",
    "CrmInternalApiBasePath",
    "PublicPagesInternalApiKey",
    "VisitSessionSecret",
    "PublicPagesBaseDomain",
    "PathTenantFallback",
    "HcaptchaSiteKey",
    "HcaptchaSecretKey",
    "GoogleMapsEmbedApiKey",
    "LimitTenantBookingsDaily",
    "CrmTimeoutMs",
    "VisitSessionTtlSeconds",
    "VisitMinFillSeconds",
    "LimitIpBurst",
    "LimitIpBurstWindowSeconds",
    "LimitIpHourly",
    "LimitIpBookingsDaily",
    "LimitPhoneBookingsDaily",
    "LimitCaptchaTrigger",
    "HcaptchaVerifyTimeoutMs",
    "AssetCacheSeconds",
    "PageCacheSeconds",
    "ApiStageName",
    "PagesApiBasePath",
    "EnableBasePathStrip",
    "LogLevel",
    "LogRetentionDays"
  ]
}
